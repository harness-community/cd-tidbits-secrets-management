# Clone podinfo, push to a *private* Docker Hub repo, deploy with
# an image pull secret injected from Harness secret dockerhub-pat.

pipeline:
  name: secrets-private-registry
  identifier: secretsprivateregistry
  # REPLACE
  projectIdentifier: <YOUR_PROJECT_IDENTIFIER>
  # REPLACE
  orgIdentifier: <YOUR_ORG_IDENTIFIER>
  tags: {}
  gitOpsEnabled: false
  properties:
    ci:
      codebase:
        connectorRef: githubconnector
        repoName: harness-community/podinfo
        build:
          type: branch
          spec:
            branch: master
  stages:
    - stage:
        name: Build
        identifier: build
        type: CI
        spec:
          cloneCodebase: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: BuildAndPushDockerRegistry
                  name: Build and Push podinfo
                  identifier: build_and_push_podinfo
                  timeout: 20m
                  spec:
                    connectorRef: dockerhubconnector
                    # REPLACE: private Docker Hub repo (same as service imagePath)
                    repo: <YOUR_DOCKERHUB_USER>/podinfo
                    tags:
                      - <+pipeline.sequenceId>
                      - latest
                    dockerfile: Dockerfile
                    context: .
                    caching: true
        failureStrategies:
          - onFailure:
              errors:
                - AllErrors
              action:
                type: MarkAsFailure
    - stage:
        name: Deploy
        identifier: deploy
        type: Deployment
        spec:
          deploymentType: Kubernetes
          gitOpsEnabled: false
          service:
            serviceRef: podinfo
            serviceInputs:
              serviceDefinition:
                type: Kubernetes
                spec:
                  artifacts:
                    primary:
                      primaryArtifactRef: podinfo_image
                      sources:
                        - identifier: podinfo_image
                          type: DockerRegistry
                          spec:
                            tag: <+pipeline.sequenceId>
          environment:
            environmentRef: podinfoenv
            deployToAll: false
            infrastructureDefinitions:
              - identifier: podinfoinfra
          execution:
            steps:
              - step:
                  name: Rolling Deploy
                  identifier: rolling_deploy
                  type: K8sRollingDeploy
                  timeout: 10m
                  spec:
                    skipDryRun: false
                    pruningEnabled: false
            rollbackSteps:
              - step:
                  name: Rolling Rollback
                  identifier: rolling_rollback
                  type: K8sRollingRollback
                  timeout: 10m
                  spec: {}
        failureStrategies:
          - onFailure:
              errors:
                - AllErrors
              action:
                type: StageRollback
